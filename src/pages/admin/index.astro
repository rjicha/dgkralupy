---
// Redirect to the static admin interface (Decap CMS)
// Uses import.meta.env.BASE_URL to work in both dev and production
return Astro.redirect(`${import.meta.env.BASE_URL}/admin/`.replace(/\/+/g, '/'));
---
